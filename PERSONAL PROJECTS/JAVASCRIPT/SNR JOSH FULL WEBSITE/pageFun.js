const menu = document.querySelector('.js-menu-icon');
const menuDIV = document.querySelector('.jc-menu-container');
const closeBtn = document.querySelector(`.close-icon`);
const openBtn = document.querySelector(`.open-icon-${con.id}`);
const answerDiv = document.querySelector(`.js-answer-${con.id}`);
const answerCon = document.querySelector(`.faq-question-${con.id}`);
const closeAnswerIcon = document.getElementById(`.close-answer-${con.id}}`);